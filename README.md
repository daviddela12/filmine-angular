# FilmineAngular applying the Hexagonal Architecture

Simple app with a list of elements and a message log showing the actions on these data.
Easily way to undestand the ports, adapters and domain inside a real problem.

